{
  "name": "express-example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "lint": "./node_modules/.bin/semistandard",
    "test": "export NODE_V8_COVERAGE=coverage; ./node_modules/.bin/_mocha --recursive --exit",
    "check-coverage": "./node_modules/.bin/c8 check-coverage --per-file true --temp-directory=coverage --lines 95 --functions 95 --branches 95"
  },
  "main": "bin/www",
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "1.9.0"
  },
  "devDependencies": {
    "c8": "^5.0.4",
    "mocha": "^6.2.0",
    "semistandard": "^14.2.0",
    "supertest": "^4.0.2"
  }
}
